@using System.Security.Claims;
@using System.Text.Json
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>@ViewData["Title"] - KidNest</title>
    <link rel="icon" type="image/svg+xml" href="~/favicon.svg" />
    <link rel="stylesheet" href="~/lib/sweetalert2/sweetalert2.min.css" />
    <link rel="stylesheet" href="~/lib/font-awesome/css/all.min.css" />
    <link rel="stylesheet" href="~/css/bootstrap.css" />
    <link rel="stylesheet" href="~/css/styles.css" asp-append-version="true" />
</head>
<body>
    <!-- Header -->
    <header>
        @await Html.PartialAsync("_Header")
    </header>

    <!-- Main -->
    <main role="main" class="bg-primary">
        @RenderBody()
    </main>

    <!-- Footer -->
    <footer class="footer bg-light border-top">
        @await Html.PartialAsync("_Footer")
    </footer>

    <!-- Back to top -->
    <a href="#" id="backToTopBtn" class="btn btn-secondary position-fixed bottom-0 end-0 m-1 p-1 rounded-3 shadow d-none">
        &uarr; Top
    </a>

    <!-- Alerts & Toast Notification Containers -->
    @await Html.PartialAsync("_Notification")

    <!-- Cart Offcanvas -->
    @await Component.InvokeAsync("CartOffcanvas")

    <!-- Modal for order confirmation, initially hidden -->
    @await Component.InvokeAsync("OrderConfirmationModal")

    <!-- NToast Notification Component -->
    @await Component.InvokeAsync("NToastNotify")

    <!-- Scripts -->
    <script src="~/lib/jquery/dist/jquery.min.js"></script>
    <script src="~/lib/bootstrap/js/bootstrap.bundle.min.js"></script>
    <script src="~/lib/sweetalert2/sweetalert2.min.js"></script>
    <script src="~/lib/elevatezoom/jquery.elevatezoom.min.js"></script>
    <script src="~/lib/microsoft-signalr/signalr.min.js"></script>
    <script src="~/js/script.js" asp-append-version="true"></script>

    @await RenderSectionAsync("Scripts", required: false)

    @if (TempData["Success"] != null)
    {
        <script>
            Swal.fire({
                icon: 'success',
                title: 'Success!',
                html: @Html.Raw(JsonSerializer.Serialize(TempData["Success"]))
            });
        </script>
    }

    @if (TempData["Error"] != null)
    {
        <script>
            Swal.fire({
                icon: 'error',
                title: 'Oops...',
                text: '@TempData["Error"]',
                footer: '<a href="/help">Need help?</a>'
            });
        </script>
    }

    @if (TempData["Info"] != null)
    {
        <script>
            Swal.fire({
                icon: 'info',
                title: 'Info',
                text: '@TempData["Info"]',
                footer: '<a href="https://localhost:7222/account/register"> Create a new account?</a>'
            });
        </script>
    }
</body>
</html>
